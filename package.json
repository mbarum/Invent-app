{
  "name": "masuma-ea-hub",
  "version": "1.0.0",
  "private": true,
  "description": "A production-grade, web-based Point of Sale (POS), inventory management, and shipping sticker generation system for Masuma Autoparts East Africa.",
  "scripts": {
    "dev": "npm run build --workspace=@masuma-ea/types && npm run dev --workspace=@masuma-ea/types & npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "build": "npm run build --workspaces"
  },
  "workspaces": [
    "frontend",
    "backend",
    "packages/types"
  ],
  "author": "AI Studio",
  "license": "UNLICENSED"
}