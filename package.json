{
  "name": "masuma-ea-hub-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for Masuma EA Hub frontend and backend.",
  "scripts": {
    "postinstall": "npm install --workspaces --if-present",
    "dev": "concurrently \"npm:dev --workspace=backend\" \"npm:dev --workspace=frontend\"",
    "build": "npm run build --workspace=@masuma-ea/types && npm run build --workspace=frontend",
    "start": "npm start --workspace=backend"
  },
  "workspaces": [
    "frontend",
    "backend",
    "packages/types"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
